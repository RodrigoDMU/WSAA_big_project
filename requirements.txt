blinker==1.9.0 # Provides fast and simple object-to-object and broadcast signaling; used internally by Flask.
click==8.2.1 # A package for creating command-line interfaces; Flask uses it for CLI commands.
Flask==3.1.1 # The main micro web framework you're using to build your REST API.
flask-cors==6.0.0 # A Flask extension for handling Cross-Origin Resource Sharing (CORS), useful for frontend-backend interaction.
itsdangerous==2.2.0 # Helps securely sign data, often used for session and cookie handling in Flask.
Jinja2==3.1.6 # A modern and designer-friendly templating language for Python, used by Flask to render templates.
MarkupSafe==3.0.2 # Escapes characters so text can be safely inserted into HTML/XML; used by Jinja2.
mysql-connector==2.2.9 # A MySQL driver for Python (older version); used for connecting to MySQL databases.
mysql-connector-python==9.3.0 # A more recent MySQL driver from Oracle for Python.
Werkzeug==3.1.3 # A WSGI utility library used by Flask for routing, request/response handling, etc.